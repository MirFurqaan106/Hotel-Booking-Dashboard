name: Trigger Render Backend Rebuild

on:
  push:
    branches:
      - master
    paths:
      - 'backend/**'

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Call Render Deploy Hook
        run: |
          curl -f -X POST "${{ secrets.RENDER_DEPLOY_HOOK_URL }}"
